<?php
/**
 * Copy this file to mail_config.php and fill in values. Do NOT commit mail_config.php.
 * PHPMailer uses SMTP (not a REST "API") — use your provider's SMTP host, port, and credentials.
 *
 * Examples: Gmail (App Password), Microsoft 365, SendGrid SMTP relay, corporate SMTP.
 */
return [
    'smtp_host'     => 'smtp.gmail.com',
    'smtp_port'     => 587,
    'smtp_user'     => 'your-sender@example.com',
    'smtp_password' => 'your-smtp-password-or-app-password',
    'from_name'     => 'SATC - SURF2SAWA',
    'base_url'      => '', // e.g. 'https://yourdomain.com/path/' or leave empty to auto-detect
];
